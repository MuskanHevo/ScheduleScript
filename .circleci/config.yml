version: 2.1

orbs:
  node: circleci/node@volatile

jobs:
  warning-probe:
    docker:
      - image: cimg/base:2024.01
    steps:
      - checkout
      - run: echo "probing for pipeline-level warnings"

workflows:
  warning-test:
    jobs:
      - warning-probe
